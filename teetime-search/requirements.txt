# Host-side dependencies for the skill scripts. Deliberately minimal —
# keyring must run natively to reach the OS keychain, and rich renders the
# terminal tee sheet.
httpx==0.28.1
keyring==25.6.0
rich==15.0.0
