# Floor pins, not exact pins: the no-Docker mode installs these into a venv
# on whatever Python the user has (3.10-3.14+), and exact pins strand newer
# interpreters without prebuilt wheels (rapidfuzz on 3.14 was the lesson).
fastapi>=0.115
uvicorn[standard]>=0.34
httpx>=0.28
pydantic>=2.10,<3
redis>=5
rapidfuzz>=3.11
