# Copy to .env. Never commit .env.
# Partner-API (app-level) credentials only. User logins live in the OS
# keychain and are passed per-request by the skill — never put them here.
GOLFNOW_CLIENT_ID=
GOLFNOW_CLIENT_SECRET=
